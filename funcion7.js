let numeros = [2, 4, 5, 37, 0];
let numeros_duplicado = {0:0, 2:2, 4:8, 5:10, 37:74};

for (let i = 0; i < numeros.length; i++) {
    console.log("Valor del indice i: "+i);
    console.log("Valor del indice i:"+i+" y el valor que tiende dentro: "+numeros[i]);
}